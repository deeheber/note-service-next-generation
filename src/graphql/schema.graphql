input CreateInput {
	author: String!
	content: String!
}

type Note {
  id: ID!
  content: String!
  author: String!
  createdAt: String!
}

type Mutation {
	createNote(note: CreateInput!): Note
}

type Query {
  # TODO actually finish this
	getNote: String
}

schema {
	query: Query
	mutation: Mutation
}